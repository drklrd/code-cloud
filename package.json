{
  "name": "code-cloud",
  "version": "1.0.0",
  "description": "NPM package to generate word cloud for your code",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsdom": "^11.0.0",
    "recursive-readdir" : "*"
  }
}
